import createGame from '@components/createGame';

import Gameplay from './Gameplay';

const instructions = 'Instrucciones del juego.';

export default createGame({instructions, Gameplay, mode: 'portait'});
